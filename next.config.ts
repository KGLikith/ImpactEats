import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["ucarecdn.com"],
  },
  // install packages forcing to use the same version as the one in th
};

export default nextConfig;
